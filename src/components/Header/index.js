import React from 'react';

const Header = () => {
    return(
        <header className="bg-danger text-white text-center py-3">
            Pokémon 
        </header>
    );
};

export default Header;